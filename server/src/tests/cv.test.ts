
import { cvService } from '../service/cvServices';
import { CV } from '../model/cv';

// test('should return a cv', async () => {
//   const cv = await cvService.getCV();
//   expect(cv).toBeInstanceOf(CV);
//   expect(cv instanceof CV).toBeTruthy();
// })
